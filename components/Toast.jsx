"use client"
import React from 'react';
import { Toaster } from 'react-hot-toast';

const Toast = () => {
    return (
        <div>
            <Toaster />
        </div>
    );
};

export default Toast;